interface ReferralFormDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  caseId: number;
  organizationId: number;
}

export function ReferralFormDialog(_props: ReferralFormDialogProps) {
  return null;
}
